# Run: supabase status -o env  (from this repo, after pnpm db:start)
SUPABASE_URL=http://127.0.0.1:54321
# CLI label: Secret (sb_secret_...) — NOT the Storage S3 secret
SUPABASE_SECRET_KEY=

# Legacy name still accepted by loadEnv():
# SUPABASE_SERVICE_ROLE_KEY=

DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

CORS_ORIGIN=http://localhost:3000
PORT=3001
PAYMONGO_WEBHOOK_SECRET=
CRON_SECRET=
